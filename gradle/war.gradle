apply plugin: "war"

bootWar {
    mainClass = "com.outis.stm.StManagementApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
